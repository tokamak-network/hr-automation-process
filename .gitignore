backend/venv/
frontend/node_modules/
frontend/.next/
__pycache__/
backend/.env
.env.local
.env*.local
payroll/backend/venv/
payroll/frontend/node_modules/
payroll/frontend/.next/
.DS_Store
backend/backups/
backend/uploads/
.env

# data stores — never commit (public repo)
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
/data/

# 발표용 pptx — 레포에 포함하지 않음
docs/*.pptx

# Gmail 읽기 자격증명·토큰 — 절대 커밋 금지 (C-1)
backend/gmail_token.json
backend/gmail_credentials.json
*gmail*token*.json
*gmail*credentials*.json

# 클라우드(실데이터) 1회성 접속 스크립트 — 비추적 (운영 DB 접근 도구)
backend/run-cloud.sh

# Gmail 토큰 발급 스크립트 — 비추적 (1회용 OAuth 도구)
backend/gen-gmail-token.py

# 로컬 전용 기동 스크립트 — 비추적 (개인 편의)
start-local.sh

# FUSE 임시 아티팩트
.fuse_hidden*

# 팀원 GitHub 핸들 (실데이터) — 코드 밖으로 외부화, 커밋 금지
backend/team_members.json
